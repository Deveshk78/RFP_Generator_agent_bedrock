# API is proxied via Next.js rewrites — no need to set NEXT_PUBLIC_API_URL in the browser.
# Optional: override backend URL for the Next.js proxy (default http://127.0.0.1:8000)
# API_BACKEND_URL=http://127.0.0.1:8000
